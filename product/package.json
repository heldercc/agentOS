{
  "name": "agentos-product-shell",
  "version": "0.0.1",
  "private": true,
  "description": "The emerging AgentOS product shell — the 13-step Product Loop (docs/PRODUCT-LOOP.md). Not the frozen rig, not yet the Kernel: the Beta of the actual product.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "shell": "tsx src/cli/shell.ts",
    "smoke": "tsx src/cli/smoke.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
