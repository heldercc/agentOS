# Blind evaluation — run fake-2026-07-11T22-08-37

> The two outputs below came from two different context strategies. You are not
> told which is which. For each task, decide which output is better (A, B, or tie)
> and record it in `verdicts.json`. Do NOT open `mapping.sealed.json` until every
> verdict is filled and committed.

---

## Task: Timing chart for shot 3 (task-timing-chart)

**Instruction:** Produce a frame-level timing chart for shot 3 (Rin's counter, 5s), applying the project's pacing rhythm and impact-frame rules. Note where the freeze-frame pause falls.

### Output A

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 6833
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

### Output B

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 3618
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

**Verdict (A / B / tie):** _____

---

## Task: Colour callout for the counter (task-palette-callout)

**Instruction:** Write the colour specification for the shot-3 rim-light moment, consistent with the project's palette judgment, arena lighting, and character design.

### Output A

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 3842
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

### Output B

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 6824
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

**Verdict (A / B / tie):** _____

---

## Task: Camera plan across the 4 shots (task-camera-plan)

**Instruction:** Specify the camera angle and movement for each of the four shots, applying the project's aggressor/defender angle doctrine and the locked shot list.

### Output A

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 6819
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

### Output B

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 2399
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

**Verdict (A / B / tie):** _____

---

## Task: Sound design for the decisive hit (task-sound-design)

**Instruction:** Describe the sound treatment for the decisive blow in shot 3, applying the project's silence-then-thud rule over the established audio bed.

### Output A

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 6814
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

### Output B

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 2737
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

**Verdict (A / B / tie):** _____

---

## Task: Reconcile timing with open ends (task-continuation)

**Instruction:** Given the locked shot list and the open questions, propose how the final freeze (shot 4) should be timed and framed WITHOUT deciding who wins. Flag every undecided fact.

### Output A

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 6842
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

### Output B

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 4173
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

**Verdict (A / B / tie):** _____

---

## Task: Pitch a title card concept (task-open-creative)

**Instruction:** Propose three original title-card concepts for the piece. This is open creative work with no prior decisions to reuse.

### Output A

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 1492
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

### Output B

# Fake Mentor output

This is a deterministic placeholder produced by the fake model so the
Beta Coding pipeline can be exercised at zero cost.

- input characters seen: 6787
- requested model: fake
- max tokens: 8000

The real Mentor's judgment arrives when the Anthropic port runs (slice 3).

**Verdict (A / B / tie):** _____

