# Seed — Impact frames: smear then hold (PLACEHOLDER)

> [PLACEHOLDER — autorar com o Pilot]

On the frame of contact, use a single smeared in-between (motion blur across the
whole limb), then hold the following pose 2 frames longer than natural. The smear
reads as speed; the hold reads as weight. Never smear two consecutive impacts.
