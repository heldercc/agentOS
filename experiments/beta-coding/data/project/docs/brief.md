# Project Brief — 15-second Fight Scene (PLACEHOLDER)

> [PLACEHOLDER — autorar com o Pilot] Stand-in project brief.

A 15-second anime-style fight scene with two original characters. Style is an
homage only — no existing IP. The deliverable is a Render Order: a specification
complete enough that any rendering system could execute it. This brief is the
kind of always-relevant document the full-reload path carries every time.
