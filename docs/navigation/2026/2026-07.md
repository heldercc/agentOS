# July 2026

## Session 001

Pilot: Helder Costa
Copilot: ChatGPT

Highlights

- AgentOS concept born.
- Executive Mode defined.
- Project State introduced.
- GuruSeed conceived.
- Mentor Expertises replace personas.
- Navigation established as the long-term architectural compass.
- Repository kept private.

Outcome

Foundation Revision 1 initiated.

## Session 002

Pilot: Helder Costa
Copilot: Claude

Highlights

- Foundation refined from skeleton to substantive documents (Revision 2).
- Full terminology defined: Pilot/Copilot roles, Kernel, Mentor, Mentor Expertise,
  GuruSeed, InjectSeed, Project State, Context, Memory, Artifact, Executive Mode,
  Navigation, Workflow, Runtime, Cost Probe.
- Conceptual architecture established: Executive UI / Kernel / Mentor Engine /
  Runtime / State Layer, with canonical flow and architectural invariants.
- ADR process formalized: template created; ADR-0000 (architecture-first),
  ADR-0001 (Navigation compass) and ADR-0002 (Expertises over personas) written
  in full and accepted.
- Roadmap restructured into phases with explicit exit criteria; Phase −1 closed.
- Incubator restructured with promotion path (incubating → candidate → roadmap).
- VISION expanded with success criteria and non-goals; PRINCIPLES expanded with
  per-principle rationale.

Outcome

Foundation Revision 2 delivered. Next: close Phase 0 exit criteria.

## Session 003

Pilot: Helder Costa
Copilot: Claude

Highlights

- Foundation recast as the Foundation Book: chapters, why before how, ten-year
  writing standard.
- Constitution (7 articles) supersedes Principles; engineering rules moved to
  architectural invariants (ADR-0004).
- Context-as-scheduled-resource made the central motivation: Chapter I opening,
  Article 5, ADR-0003.
- Revision 3 layer chain reviewed and rejected as a category error; three-plane
  architecture adopted; Navigation placed explicitly outside the runtime.
- Mentor/Agent contradiction resolved: Mentor is the only Agent in Foundation;
  sub-agent hierarchies to Incubator.
- Effort Probe supersedes Cost Probe with mandatory calibration loop (ADR-0006);
  ROI modeling sent to Incubator.
- Seed Resolver renames InjectSeed (ADR-0005).
- VISION absorbed MANIFESTO; vendor names removed from founding text.
- Full architectural review recorded in docs/reviews/.

Outcome

Foundation Revision 4 delivered.

## Session 004

Pilot: Helder Costa
Copilot: Claude

Highlights

- Copilot transition formalized; founding Copilot (ChatGPT) produced a
  knowledge export of the founding sessions, preserved verbatim in
  docs/heritage/.
- Reconciliation performed: Revision 4 had conflated teaching with resolution;
  the founding teaching mechanism restored as Seed Composer (ADR-0007,
  Proposed, awaiting Pilot ratification). ADR-0005's context section corrected
  by record.
- Seed lifecycle upgraded to founding 7-state sketch (idea → draft → candidate
  → approved → core → deprecated → archived).
- GuruSeed founding anatomy preserved in TERMINOLOGY; Executive Health Check
  to Incubator; repository named Book I of the four-book arc.
- Confirmed: Revision 3 never existed as files — only decisions; Session 003's
  brief-based review method validated retroactively.
- Pilot fixed strategic objectives: (2) internal factory producing verticals,
  (3) expertise/case-study as sellable asset. First production line: original
  anime-style action short (original characters — style homage only, no
  existing IP).

Outcome

Foundation Revision 5 delivered. Phase 0 closes upon Pilot ratification of
ADR-0007 and disposition of remaining open questions.


## Session 004 — addendum (primary objective)

- ADR-0007 (Seed Composer) ratified by the Pilot: status Accepted.
- Pilot registered the PRIMARY OBJECTIVE: the AgentOS App — daily governed
  refinement across sessions; Pilot creates/selects/improves Mentors; system
  proposes per-layer detail; Pilot evaluates/approves/deletes; scheduler
  reuses approved decisions to economize tokens. Product = the Render Order
  (complete, renderer-agnostic spec), not the rendered video.
- Consequence: build order inverted — App (Kernel + Executive loop, via
  Claude Code) precedes any rendering stack. ComfyUI/GPU decisions parked.
- First objective for the App: 15s anime-style fight, original characters.


## Session 004 — Phase 0 close

- Pilot fixed the deepest premise: AgentOS is the ENGINE, not the fight. The
  engine is product-agnostic and the workers themselves are governed data —
  recorded as ADR-0008 (Accepted) and as two new architectural invariants.
- Roadmap (Chapter V) rewritten as the build plan toward the AgentOS App:
  Kernel → Executive Loop → Scheduler & Reuse → Render Order → Agnosticism
  Proof → Self-Improvement. Rendering explicitly outside the roadmap.
- Open questions dispositioned (1 decided: local-first via Claude Code;
  4 formally deferred with owners and deadlines).
- Phase 0 declared COMPLETE. Foundation Revision 7 is the closing revision of
  Book I. Next: code.

Outcome

Foundation Book I complete. Phase 1 (Kernel) authorized to begin.


## Session 004 — Book II charter

- Pilot fixed the learning loop: the system observes recurring success
  (outputs/features selected repeatedly), distills candidate seeds with
  evidence, and the Pilot validates before incorporation into agents or the
  system. Two seed origins (taught / learned), one gate: the Pilot.
- Recorded as ADR-0009 (Seed Harvester — name proposed by Copilot, Pilot may
  rename); "Automatic Seed Evolution" promoted from Incubator per its own
  promotion path. Seed pipeline complete: Composer · Harvester · Resolver.
- Chapter VI (OBJECTIVES.md) created: nine concrete, testable objectives the
  code must prove (O1 daily loop … O9 agnosticism proof). Every future demo
  is measured against this list.
- ADR-0007 references updated to Accepted throughout.

Outcome

Foundation Revision 8. Book I closed with Book II chartered. Next session
writes the Kernel.


## Session 004 — SavePoint integration

- Public repo inspected: founding Copilot's "SavePoint R5 Proposal" (structure
  placeholders) + phone-commit debris found. Proposal preserved in heritage
  and formally reviewed (docs/reviews/2026-07-REVIEW-SAVEPOINT-R5.md).
- Adopted: engineering/ skeleton (Book II home), five-verdict review taxonomy.
- Rejected on record: second canonical tree, era-based roadmap, NOT_YET
  rename, separate philosophy folder.
- Constitution Candidate pending Pilot ratification: "Complexity must justify
  itself" (proposed Article 8).
- Repo visibility noted as PUBLIC — contradicts the Session 001 privacy
  decision; awaiting the Pilot's explicit call.


## Session 004 — Article 8 & first push

- Pilot RATIFIED Article 8: "Complexity must justify itself." Constitution now
  has eight articles.
- Pilot instructions recorded: (a) a Constitution improvement pass precedes
  coding; (b) revisit the SavePoint structural rejections when indexing work
  begins; (c) documentation generosity affirmed — "documentação nunca é demais".
- Remote repository reset: clean lineage force-pushed to
  github.com/heldercc/agentOS, superseding phone-commit debris (content
  preserved in heritage).


## Session 004 — Stack decision

- ADR-0010 ratified by the Pilot: TypeScript (strict, Node LTS) for engine
  and App; Tool Runner (JSON stdin/stdout contract) makes tools
  language-agnostic — Python as first external citizen (and ComfyUI's future
  entry door); native modules only where the Effort Probe proves the need
  (Article 8).
- Rust/Go rejected for the engine (optimizing microseconds in a
  human/API-bound system); Python engine rejected narrowly (typing discipline
  + one-language system).
- Phase 1 is now fully unblocked. Next session: kernel/ in code.


## Session 004 — Expertise curation begins

- Pilot mandate: devil's-advocate curation of claude-skills (355 skills, MIT).
- Admitted 2 (reflect, capture) into expertise/imported/ with provenance;
  full verdicts in docs/reviews/2026-07-REVIEW-SKILL-IMPORT.md.
- Seed origins extended to three: taught · learned · imported — one gate.
- Insights harvested from rejects: seed-hygiene checklist; Composer import
  mode (both to Incubator).


## Session 004 — Reconciliation of Revision 8

- External review of R8 received (preserved in heritage) and reconciled via
  decision table (docs/reviews/2026-07-RECONCILIATION-R8.md).
- Accepted & applied: Resolver as attributable/auditable (determinism
  aspirational), Project State as smallest sufficient description, provenance
  by reference, per-store versioning policies, O7 vs. full-reload baseline,
  vendor removed from roadmap phase, Vision absolutes softened.
- Constitution amended: Article 6 → "Architecture Precedes Commitment"
  (ADR-0011, ratified by the Pilot).
- Rejected with history verified: GuruSeed→Seed rename and Records Plane —
  both misattributed to R4; both previously decided on record.
- Book I declared reconciled and stable. Book II opens with the single
  vertical experiment (work order → Artifact with metering, full-reload vs.
  scheduled context).

## Session 005 — Book II opens (2026-07-11/12)

Pilot: Helder Costa
Copilot: Claude (Fable 5; Sonnet 5 early in session) — via Claude Code on the Pilot's PC

Highlights

- ADR-0012 rig completed and driven end-to-end on the fake model at zero cost;
  ADR-0013 manual model port (subscription Claude Codes, no API wallet) with
  conductor and one-click launcher.
- Local browser dashboard (http://localhost:4600): one-button comparison runs,
  live progress, blind A/B judging with buttons, RESULTS.md rendered in place;
  hosts the manual-model mailbox worker itself. Claude Code CLI installed.
- Constitution gains Article 9 — Certainty Precedes Action (ADR-0014):
  below 90% confidence in governing intent, stop and interview the Pilot.
  Threshold 95→90 revised by the Pilot; scope (dev-time + runtime) interviewed.
- Book II engineering plan (engineering/OVERVIEW.md) answers the founding GPT
  brief (preserved in heritage), reconciled against ratified Book I: slice
  already built, criteria stay frozen, Scheduler stays undesigned until O7
  evidence, Tool Runner deferred, repo structure unchanged.
- Expertise store grows 2 → 13 artifacts: risk-beside-cost + adversarial-review
  (ISO 42001 audit, review on record); circuit-breaker + authority-matrix
  (guardrails article; MindStudio career piece rejected 6/6); fishing
  expedition 1 (3 Sonnet trawlers, Fable 5 camera gate): 24 candidates → 6
  admitted, incl. just-in-time-context which sharpens O7 (the scheduler may
  WIN on quality, not merely hold it).
- Incubator: Researched Seeds — the fertile first garden (pre-project
  state-of-the-art research as a project's base seed layer, through the gate).
- ISO/IEC 42001 verdict: not constitutional text; three gaps routed to owed
  ADRs; "aligned with" is claimable, "certified" is not without a paid audit.
- Ops: C: drive hit 0 GB free mid-session; Steam moved to D: (~31 GB freed);
  cleanup continuation notes in the Pilot's Documents folder.

Outcome

Book II open, hypothesis-first. The rig awaits evidence: the Pilot's real
fight-scene corpus, one real manual run, blind verdicts, then the requested
devil's-advocate review of the results.

## Session 006 — Expedition 2 gated; Trawler+Camera dispositioned (2026-07-12)

Pilot: Helder Costa
Copilot: Claude (Fable 5; Sonnet 5 at session start) — via Claude Code on the Pilot's PC

Highlights

- Fishing Expedition 2 (GPT net, same three waters, corrected prompt):
  10 candidates → 2 admitted, 3 noted to owed ADRs, 5 rejected. Review:
  docs/reviews/2026-07-REVIEW-FISHING-EXPEDITION-2.md. Admitted:
  pointwise-before-pairwise (the eval protocol can fabricate the winner;
  arXiv 2406.12319 + 2504.14716) and knowledge-freshness (freshness is a
  system operation; OpenAI Cookbook temporal agents). Expertise store 13 → 15.
- Catch-quality findings recorded: GPT violated the SOURCE format (no URLs —
  gate verified sources independently); 4/10 drops were one correlated
  routing cluster; water 3 (expertise capture) came back empty; the
  21-finding exclusion list held (zero overlap).
- GPT's Trawler+Camera architecture proposal reviewed adversarially
  (docs/reviews/2026-07-REVIEW-TRAWLER-CAMERA-PROPOSAL.md). Verdict: the
  architecture is already ours (expedition-1 pattern, Pilot's invention);
  adopted four zero-infrastructure controls — expedition manifest,
  cost-per-admitted-drop metric, counterevidence query families, dedup as an
  explicit gate step; build nothing (Article 8). No new ADR, no incubator
  entry.
- First expedition economics on record: exp 1 ≈ 25k tokens per admitted
  drop; exp 2 ≈ 0 marginal (GPT subscription) + gate. Same-waters returns
  are diminishing; next cast waits for a new hungry water.

Outcome

The net is retired until new ADRs open new waters. Unblocked path unchanged
and data-first: the Pilot's real fight-scene corpus → anti-bias review
(engineering/OVERVIEW.md §6) → one real manual run on the dashboard → blind
verdicts → RESULTS.md → devil's-advocate review.

Addendum (same session, later)

- The Pilot registered the TWO GRAND OBJECTIVES, now explicit at the top of
  docs/OBJECTIVES.md: (A) token economy — the Kernel, pure engineering,
  scale-survival (O7 + ADR-0003/0006/0012); (B) taste governance by clicks —
  the user's daily loop, selection as teaching signal, no API / no
  fine-tuning, learning embedded in the UI as seeds + selection evidence
  (O1–O6, ADR-0013 spirit). Hierarchy rule: when priorities collide, these
  two win; every owed ADR names its pole.
- ADR-0015 drafted and committed as PROPOSED: the Beta 2 governance question
  ("can governed selection measurably improve what the system proposes —
  taste captured as seeds, entirely through clicks?"), ADR-0012 discipline
  (question frozen, committed No/Yes/Depends exits, anti-moving-goalposts).
  Awaits the Pilot's ratification. Order preserved: Beta 1's real run first.
- Fan-out economy call (delegated to the Copilot): no subagents spawned for
  this work — doctrine writing is context-heavy and the context was already
  hot in-session; fan-out is reserved for context-light parallel search
  (expeditions). Registered as the standing pattern.

Addendum 2 (same session — "siga tudo fast")

- ADR-0015 RATIFIED by the Pilot (one-line concession registered in the ADR:
  rig construction starts immediately; Beta 1's unblocker is the Pilot's own
  data and the rig work is independent).
- Beta 2 rig BUILT and VERIFIED: experiments/beta-governance/ — blind
  proposal rounds (one per approach, sealed mapping), pointwise-first judging
  enforced server-side (selection only after every option is judged in
  isolation, only among approved — the pointwise-before-pairwise seed applied
  as code), append-only evidence log (every click an event), distiller
  (≥2 wins → candidate seed with evidence), tray (admit/discard — O5),
  admitted seeds enter later manifests (O4 loop closed), metrics (approval
  rate by round, rounds-to-selection). Governance dashboard at
  http://localhost:4700 (npm run dashboard).
- Verified at zero cost, ADR-0012 discipline: smoke = 15/15 assertions on the
  fake model (one smoke run kept as committed evidence, scripted + sandboxed);
  TypeScript strict clean; check:domain OK (engine domain-blind, ADR-0008);
  UI driven end-to-end in a real browser — clicks logged with approachId
  resolved server-side, selecting a rejected option correctly refused,
  mapping revealed only at close. The verification session's clicks were
  DELETED from runs/: they were the Copilot's judgment, not the Pilot's, and
  the evidence log admits only the owner's clicks.
- src/model.ts and src/stores.ts copied from the Beta 1 rig with provenance
  headers — rigs are disposable science and stay uncoupled (no imports across
  experiments).

Addendum 3 (same session — expedition 3 gated; ADR-0016)

- Fishing Expedition 3 (GPT net, ADR-0015 waters) GATED: 6 candidates → 0
  seeds, 3 absorbed into ADR-0016, 3 research notes (co-creator-vs-editor →
  App shell ADR; subgoal compaction → Scheduler eval post-O7; judge
  specificity → future automated camera). Review:
  docs/reviews/2026-07-REVIEW-FISHING-EXPEDITION-3.md. The Pilot's live
  reframe is registered doctrine: no psychology — "if the evaluator's inputs
  change during the experiment, the metric stops identifying the cause."
- ADR-0016 RATIFIED and IMPLEMENTED same night (timing legitimacy: zero real
  evidence existed yet): choice-set provenance (present events with order,
  content hashes, active seed versions), recorded order randomization, blind
  anchor repeats excluded from learning (Consistency reported separately
  from Improvement), pre-registered corpus per cohort, pilot_note channel
  (owner initiates direction; enters next round's context), Article 9 at
  runtime in the proposal prompt (unclear governing intent → "Perguntas ao
  Pilot" section + explicit assumptions). Smoke grew to 20/20 assertions.

Addendum 4 (Session 007, morning — the multi-project hold verified)

- The night session ran out of tokens mid-flight; its uncommitted work was
  picked up, verified and committed this morning: the governance rig now
  hosts several projects side by side (data/projects/<id>/,
  runs/<projectId>/<session>/), first-cargo migrated as the placeholder
  project, dashboard grew a project selector. Verified before commit:
  tsc strict clean, smoke 20/20, dashboard healthy end-to-end (an apparent
  /api/state hang was a PowerShell 5.1 HTTP-client quirk, not the server —
  curl answers instantly).
- goku-ssj2 (the Pilot-ordered test cargo, first REAL manual round generated
  for d-buildup, one proposal invoking Article 9 at runtime) is HELD
  uncommitted pending the Pilot's flagged morning review (IP note in its
  project.json).

Addendum 5 (Session 007, night shift — the Operating Model arrives; ADR-0017 built)

- The Pilot handed down the Operating Model verbatim (docs/OPERATING-MODEL.md,
  DOCTRINE): AgentOS is a governed operating layer, not a chat app / dashboard /
  wrapper; automation below the authority line, human judgement above it; the
  Implementation Test (SS13) is the standing anti-drift filter.
- SS11 applied on arrival: the night draft pre-authored the answer, so it was
  deleted before ever being committed. The scene is conceived inside the loop.
- ADR-0017 RATIFIED (the Pilot's directive) and BUILT the same night:
  five hard-coded effort levels (src/effort.ts) controlling model, fan size,
  critic pass, context budget, tokens, retries, timeout; the Effort Probe
  estimates before execution from real past meters and writes
  effort-actual.json after (estimate-vs-actual, the probe's report card);
  effort slider + probe estimate beside Propor in the dashboard; governed
  auto-iteration (src/auto.ts) that opens round 1 and iterates fully-rejected
  rounds but never judges, never selects, never spends above balanced;
  aggregated "Perguntas ao Pilot" card (SS4: the visible interview is singular).
- Verified at zero cost: smoke 15 -> 33 assertions green on the fake model;
  tsc strict clean; check:domain OK; driven end-to-end in a real browser on
  the placeholder project (auto opened all round-1s and stopped at the
  owner's judgment); the Copilot's UI-test run deleted from runs/.

Addendum 6 (Session 007, dawn — four real rounds on the table; the Product Loop registered)

- The first REAL evidence table is set: ascensao-t2 rounds 1 of d-buildup,
  d-eruption, d-signature and d-camera - three blind proposals each, sealed
  mappings, manual port at balanced effort. The worker CLI was not logged in,
  so the ADR-0013 human-in-the-middle path carried all 12 jobs (the Copilot
  produced, never judged - the blind against the judge holds). Auto-iteration
  opened rounds 2-4 unattended, exactly as designed, and was disarmed after
  d-camera. The probe's confidence climbed low -> medium -> high as real
  meters accumulated. Zero judgments in the log: the clicks are the owner's.
- The Pilot's next directive registered VERBATIM as doctrine
  (docs/PRODUCT-LOOP.md): the rig freezes (evidence infrastructure, done);
  the next implementation target is the PRODUCT loop - Project Init from
  free text, Claude Code via PowerShell as runtime, Question Needs aggregated
  by the Kernel, one governed question at a time, candidate Project State
  with user approval, artifacts returning automatically. User = Pilot: no
  hidden second role, governance is the normal product interaction.
- Placement decided explicitly (the directive demands it): experiment frozen
  in experiments/beta-governance/; product shell in a NEW top-level product/
  folder, reusing proven mechanisms by copy-with-provenance; Kernel design
  stays in engineering/ Book II. The shell build starts next session on a
  full tank.
- Morning path for the Pilot: open localhost:4700 (npm run dashboard if
  down), pick Ascensao T2, judge the four rounds pointwise, select winners,
  answer the aggregated questions via notes; run "claude" once in a terminal
  to log the CLI in before arming auto again.

Addendum 7 (Session 008, night - the product shell is born)

- The Product Loop got its body: a NEW top-level product/ folder, exactly as
  the placement decision demands - the rig stayed frozen, nothing was
  retrofitted, proven mechanisms entered by copy-with-provenance (stores,
  evidence, ADR-0013 runtime boundary, ADR-0017 effort, ADR-0012 manifests).
  ADR-0018 records the decisions.
- All 13 steps run end-to-end: Project Init from free text, roster convened
  by a work order, bounded consults, Question Needs aggregated/deduped/ranked,
  ONE governed question at a time, answers auto-re-consulting the asking
  agents, candidate Project State, approval/rejection-with-direction,
  governed execution behind the effort probe, artifacts auto-returning,
  iteration advance. Smoke 33/33 on the fake runtime; tsc strict clean.
- The shell face at localhost:4900 (npm run shell in product/): one primary
  action per stage, effort slider + probe before every spend, evidence tail,
  pilot-note channel. Browser-verified as the user on a fake-runtime test
  project: the probe's confidence grew low -> high on the project's own
  meters and recommended balanced on iteration 2. A real defect was found
  and fixed in the pass (quiet polls no longer eat clicks).
- The claude CLI is now installed AND logged in (the Pilot's own click, as it
  must be). The account's token window opens hours from now; the first REAL
  test of the cli runtime (haiku, short project) is scheduled for the end of
  the night, Copilot on watch while the Pilot sleeps.
- Honest note: tonight's loop verification ran on the fake runtime; no real
  model has yet flowed through product/. The cli runtime path (PowerShell
  spawn, stdin prompt, stdout artifact) is the rig's proven worker pattern,
  but it earns "verified" only when the token window opens.

Addendum 8 (Session 008, end of night - the first REAL run through the product)

- The token window opened and the cli runtime carried its first real project
  end-to-end: "Brinde de Casamento" (a 60-second wedding toast, PT-PT), haiku
  at low effort, 14 work orders, zero manual prompt transport. The user's
  founding intent went in; a complete, usable artifact came out - the toast
  uses every answer given (the Excel quirk, the missed train, the Rome
  anecdote verbatim, the bride's counterpoint) and lands the memorable final
  line the intent asked for.
- What the real model proved that the fake never could: the roster came back
  project-specific and in the user's language (Redactor de Brinde, Guarda da
  Voz Portuguesa, Engenheiro de Timing, Arquitecto de Brinde); the interview
  asked real, material questions; the candidate Project State folded all
  eight answers into decisions and constraints faithfully; the probe's report
  card caught its own miscalibration honestly (tokens +84%, duration +285% -
  it had only fake-runtime meters to learn from; the real meters now on disk
  correct it).
- Findings for the next session, all product learnings, none blocking:
  (1) with a real model the interview GROWS - agents ask follow-ups on
  re-consultation (6 -> 8 questions); it drained naturally here, but the
  user needs a governed "enough questions, build the state" exit (doctrine
  already says it: interview to unblock, not for completeness);
  (2) normalized-text dedup misses semantic duplicates across agents
  (pt-pt-voice re-asked the anecdote question in other words);
  (3) execution fan-out gives every agent the same "produce THE artifact"
  instruction - the validator agent, seeing the writer's artifact already in
  context, endorsed it by returning it byte-identical; role-shaped execution
  outputs (or an explicit refinement pipeline) is the improvement.
- The Pilot visited the shell before sleeping (project "teste", 03:20, left
  intact in his workspace). Session total: three commits pushed during the
  night plus this one; the rig untouched; the Songoku Beta inside the
  product is now unblocked for real.

Addendum 9 (Session 009, morning - the Foundation Correction, registered and built)

- The Pilot corrected the foundation, verbatim in docs/FOUNDATION-CORRECTION.md:
  AgentOS is not primarily a multi-agent system; it is a governed system for
  capturing, organising, selecting, applying and evaluating human
  intelligence. Models are interchangeable engines. Agents are temporary
  vessels. Human expertise is the durable asset. The workflow that functions
  but is not understandable to the user is incomplete.
- ADR-0019 built the same morning, smoke 33 -> 43, tsc strict, verified in
  the browser against the REAL project of the night:
  (1) Expertise store - candidate/admitted/discarded, project-local vs
  reusable, provenance, and the automatic appliedIn trail; the Kernel never
  admits, every transition is the user's evidence event.
  (2) Composition - every work order is now mandate + project context +
  applicable admitted expertise + effort budget; expertise enters before
  other optional context (judgement outranks history) with its selection
  reason in the manifest.
  (3) The story - storyOf() assembles the whole project from disk; the UI
  grew from one technical page into a workspace: Agora / Historia /
  Expertise. The timeline shows intent verbatim, every Q&A, every
  contribution with mandate/effort/model, governance moments, outputs as
  excerpts, and a chip per contribution showing the expertise received and
  why. Operational provenance only - the shell stores no model reasoning.
- Live proof on the real cli runtime: the Pilot-style user registered
  "Discursos falados: frases curtas ganham" as reusable expertise, admitted
  it, convened iteration 2 of brinde-de-casamento - the judgement entered
  both real consults' manifests with reasons and the trail recorded both
  work orders. The brain is accumulating.

Addendum 10 (Session 010, midday - the Pilot's audit; the Foundation restored;
Session 011 closes the books)

- The Pilot audited the product shell against Book I and delivered the
  verdict registered verbatim in docs/reviews/FOUNDATION-TO-PRODUCT-GAP-AUDIT.md:
  the shell shipped the peripheral circuit and called itself the product
  before wiring the central organ - reusable human intelligence - and never
  made legibility a condition of completeness. Eight numbered failures,
  eight corrective measures, a product freeze until the gap closes.
- ADR-0020 RATIFIED and registered the same session: the Foundation is
  sovereign, later doctrine is application; the minimum complete product is
  redefined (no loop is complete without expertise scheduling); Article 9
  reconciled (blocking uncertainty asks, non-blocking records assumptions,
  the Pilot may declare context sufficient); workflow legibility as an
  invariant; Mentor naming restored; the Human Intelligence Library
  gitignored in the public repo (its privacy is the Pilot's call, surfaced
  not silent). Two doctrine docs arrived verbatim:
  docs/GOVERNANCE-INTERACTION-MODEL.md and docs/HUMAN-INTELLIGENCE-LIBRARY.md.
- The corrective Human Intelligence slice was BUILT the same session
  (smoke 43 -> 52, tsc strict): src/hi.ts (GuruSeeds on disk with the full
  anatomy, Mentors as user-authored compositions, candidates admitted only
  by the user's hand, mechanical migration from the ADR-0019 flat store -
  the brinde judgement now lives at seeds/general/ with its trail intact,
  and the first Mentor, guardiao-da-palavra-falada, exists on disk);
  src/resolver.ts (per-Work-Order Seed Resolver with legible reasons and
  Mentor attribution); Decision Surfaces (options fanned from the user's
  Mentors first, refine-with-lineage, candidate GuruSeed extracted from
  refinement, selection closes the surface as evidence); the Expertise tab
  reborn as Inteligencia Humana (Mentores / Candidatas / GuruSeeds).
- The session's account ran out of tokens right after the push, before the
  diary and compass could close. Session 011 picked the thread up, verified
  the claims independently - tsc strict clean, smoke 52/52 green - and
  wrote this record. The commits were honest.
- Honest note: the slice is smoke- and browser-verified only. It has NOT
  yet flowed through the real cli runtime (workspace untouched since the
  06:38 real run). The live proof - the Resolver selecting the migrated
  seed into a real work order, provenance on the artifact, evidence
  returning to the seed - is the next owed act before the Songoku Beta
  resumes (ADR-0020, Consequences).

Addendum 11 (Session 011, afternoon - the slice completes and PROVES ITSELF LIVE)

- The two open critical rows of the gap audit got their bodies (smoke
  52 -> 58, tsc strict): every executed artifact now writes a provenance
  sidecar - work order, model, effort, and every GuruSeed that shaped it
  with id, version, the Resolver's reason and Mentor attribution - while
  the artifact text stays byte-faithful to the runtime's output; and the
  user's verdict now returns to the seed (recordSeedEvidence: seed.yaml +
  append-only evidence.jsonl + an evidence event, actor pilot, never
  inferred from silence). UI: brain chips on artifact cards; evidence
  counts and "Reforcou / Contradisse" on every admitted GuruSeed.
- THE LIVE PROOF RAN, end to end, on the real cli runtime (haiku, low,
  project prova-da-slice-hi, Copilot as user per the standing directive):
  real roster in PT (Redator de Discurso, Otimizador de Timing, Polidor
  de Qualidade, Coach de Entrega), real interview (grew 5 -> 6 under
  re-consultation, drained), candidate faithful to every answer, approved,
  executed. The artifact came back WITH its sidecar naming the migrated
  seed ("Discursos falados: frases curtas ganham" v1, reason "universal
  admitted judgement; carried by Mentor Guardiao da Palavra Falada") -
  and the text visibly obeys the rule: every sentence one breath, the
  single pause before the close, ~70 words. The user's verdict went back
  through the new UI and landed on the seed: seed.yaml evidence.supporting,
  evidence.jsonl, story moment "A minha evidencia voltou a GuruSeed".
  The ADR-0020 gate is SATISFIED: the full corrective loop is proven on
  a real model. The Songoku Beta is unblocked.
- The interview growth reproduced addendum 8's finding live - so the
  owed exit finally got its body (smoke 58 -> 64): "Chega - constroi com
  o que tens" (ADR-0020 sec. 3) defers every open question (kept on file,
  never dropped), moves the stage on, and the candidate build must list
  them as unresolved, assuming nothing. The act is the user's alone.
- Honest notes: (1) the low-effort haiku artifact carries a grammar slip
  ("Vocês somos") - effort doctrine working as designed, not a defect;
  (2) semantic dedup (addendum 8, finding 2) and role-shaped execution
  outputs (finding 3) remain owed; (3) the browser screenshot tool hung
  during the pass, so the visual proof is the page text captured in the
  transcript plus the files on disk.

Addendum 12 (Session 012, evening - the audit side's parecer lands:
provenance made immutable, the proof declaration corrected)

- The audit side (the Pilot + GPT, reading origin/main) returned its
  parecer on Session 011. The previous worker session was receiving it
  when its tokens ran out - it had already pushed everything, the tree
  was clean at a83ddc2, nothing was lost. This session executed the two
  corrections the parecer ordered.
- CORRECTION OF THE PROOF DECLARATION. Addendum 11 concluded "the
  ADR-0020 gate is SATISFIED". The precise claim is narrower: the
  MECHANICS passed on a real model (real cli runtime, haiku/low) - but
  the user in the loop was the Copilot acting under the standing test
  directive, not the Pilot; and the "Chega - constroi com o que tens"
  act has only ever passed on the FakeRuntime (smoke). The system has
  never been governed by its owner. So: gate mechanically satisfied,
  HUMAN proof still owed - the Pilot governing, his taste, his "Chega",
  in the first true Beta. CURRENT.md now says exactly this.
- IMMUTABLE PROVENANCE (the correction in flight when the last session
  died). Before: seed.yaml was rewritten in place on every application
  and every evidence verdict; a Mentor save overwrote the previous
  composition; "seed v1" named in an artifact sidecar was a claim with
  nothing recoverable behind it. Now (src/hi.ts, kernel.ts):
  - Version history, write-once: seeds/<domain>/<slug>/versions/vN.yaml
    and mentors/history/<id>/vN.yaml, written with flag wx - a version,
    once written, can never be overwritten. reviseSeed / reviseSeedGoverned
    bump the version (new event seed_revised, actor pilot, in the story
    as "Revi uma GuruSeed"); getSeedVersion / getMentorVersion recover
    any version exactly as written. saveMentor snapshots pre-correction
    records before bumping.
  - Telemetry OUT of the versioned content: the application trail lives
    in applications.jsonl and the Pilot's verdicts in evidence.jsonl,
    both append-only sidecars, hydrated onto the record at read time -
    the UI sees the same fields as before, but seed.yaml now carries
    only the judgement itself plus its content_hash. A version's hash
    can no longer drift under use.
  - Hashes make the declaration verifiable, not just legible: every
    artifact provenance sidecar now records artifactSha256 (the text as
    stored), manifestSha256 (the work order's context manifest as
    written) and each applied seed's contentHash.
  - ensureImmutableProvenance(): one-time, mechanical, idempotent
    correction, runs at shell start next to the ADR-0019 migration. The
    REAL library migrated this session (2 records): the brinde seed -
    16 applications moved to the sidecar, content_hash 65ec3f6c...,
    versions/v1.yaml snapshotted - and the Mentor
    guardiao-da-palavra-falada (history/v1.yaml). Hydration verified
    live: the record still shows 16 applications and 1 supporting
    evidence, now from the sidecars.
  - Doctrine updated where it specifies the layout:
    docs/HUMAN-INTELLIGENCE-LIBRARY.md sec. 1.1 (the immutability
    contract). Smoke 64 -> 72 (checks 22a-22h: no telemetry in
    seed.yaml, v1 recoverable, revision preserves v1, hashes real and
    version-specific, version files immutable on disk, artifact/manifest
    hashes verify against disk, mentor history recoverable, the revision
    moment in the story). tsc strict clean.
- Registered from the parecer, NOT decided here (the Pilot's calls, in
  his order): where the HI library lives durably (private repo vs local
  versioned storage with backup - gitignored here, must be shared across
  Copilots without exposing the asset); the first true Beta is the anime
  fight with ORIGINAL characters (Aran, no borrowed IP), initiated by
  the Pilot in the App, with the measurement list in CURRENT.md; then
  fix what the Beta exposes (semantic dedup, role-shaped outputs); then
  the first researched Reference Mentor (Spielberg-informed Directing:
  sources, linked claims, confidence, scope, contradictions, Pilot
  admission); Kernel v0 in engineering/ only after that evidence.
- Honest notes: (1) seed revision exists at the store and governance
  layers (proven by smoke) but has no UI button yet - it arrives when
  the Beta demands it, not before; (2) provenance sidecars from Session
  011's live run predate the hashes and do not carry them - they remain
  valid as legible declarations, only new artifacts are hash-verifiable;
  (3) another session's shell was live on :4900 during this work and was
  deliberately left untouched - the Pilot has pending judgements there;
  the migration touched only the library files, which the running shell
  reads per call.

Addendum 13 (Session 012, late afternoon - the visible journey and the
project's whole life, before the Pilot's first true test)

- The Pilot chose today's first human test himself: short children's
  stories for his nieces, built INSIDE the product from one minimal
  sentence, three stories in sequence (learn -> reuse -> respect scope).
  Nothing pre-authored; the Copilot only puts the App up and observes.
  The old shell (pre-correction code) was replaced on :4900 with the
  Pilot's approval; the App was verified live - hydration through the
  real UI, zero startup errors, no project prepared.
- Before the test, the Pilot + GPT ordered one precedent correction:
  the engine had a six-moment loop but the interface did not conduct -
  technical stage badges as primary language, governance mixed with
  diagnostics, and NO formal life for a project (no conclude, archive,
  reopen). Executed as a minimal usability pass (commit dfbb0bd, smoke
  72 -> 77, verified live in the browser):
  - The persistent journey in human language - Intencao -> Compreender ->
    Decidir -> Aprovar -> Criar -> Avaliar e aprender -> Continuar ou
    concluir - mapped over the internal stages; consult/candidate/execute
    stop being primary language and live in Detalhes/Auditoria with
    runtime, agents, work orders and events. The journey orients; the
    Kernel still decides which steps are materially necessary.
  - ONE dominant card per stage: "O QUE PRECISA DE TI AGORA", with human
    titles and a semantic accent - blue for actions that launch the
    system, amber for decisions only the user can make, green for the
    archive, grey while the system works ("nao precisas de fazer nada").
  - Colour got function everywhere: approve buttons green, errors red-
    bordered, past collapsed and grey. The home page now answers the
    three questions per project: where it stands (journey step), when it
    last moved (relative time), what its next action is.
  - The project's whole life exists: "Melhorar" is a new pass with the
    Pilot's direction in front (the note enters all following context,
    decisions preserved - never an isolated step); "Concluir" is a
    governed act (review of pending candidate seeds surfaced, freeze,
    archive - NEVER delete; project_concluded event, actor pilot);
    reopening restores the loop and leaves project_reopened evidence.
    Concluded projects refuse governance ops until reopened (guard on
    every mutating route). Exercised live on the disposable smoke
    project: conclude with note -> all-green journey + archive card ->
    reopen -> loop back, everything preserved.
- Honest notes: (1) "Avaliar resultado" is guidance on the card plus the
  existing evidence/candidate flows in Inteligencia Humana - a dedicated
  per-artifact evaluation surface was NOT invented today; if the test
  shows the Pilot hesitating there, that is a finding, not a surprise;
  (2) the passive instrumentation for the test is the evidence log +
  work orders + meters already on disk - hesitation observations will
  come from the Pilot's own account and the Copilot watching server
  logs, not from new tracking.


## Session 013 addendum (2026-07-12, night) — the first human test judged the product, and the product grew a dojo

**What happened before the session:** the Pilot ran his FIRST true human
test alone — "teste teste, historia estranho de uma criança xD", a
children's story from one messy founding sentence, start to CONCLUDED in
100 minutes: 23 questions raised, 8 answered, 15 deferred by his own
"Chega" (the sufficiency act passed its HUMAN proof), 2 Decision Surfaces
decided, 2 artifacts, ~27k tokens (estimated meters), 20 work orders, all
at effort low. The measurement extractor (below) read all of this from
disk after the fact — the loop of information is closed.

**The parecer (docs/reviews/PARECER-PILOTO-2026-07-12-NOITE.md):** ten
points, A–J. Doubts were interviewed, per the Pilot's explicit directive
("qualquer dúvida, pára, entrevista-me"): the expert entity is baptized
**SENSEI**; effort becomes a per-phase profile + per-launch override; the
base guild starts with 5 crafts; Absorption (photos/video/interviews)
goes to the ROADMAP.

**Built and proven (3f9638b, smoke 88/88, tsc strict):**
- **The Sensei reform (A/B/C/F2).** A GuruSeed belongs to exactly ONE
  Sensei; the Resolver convenes seeds only through the Sensei whose craft
  overlaps the agent's tags — universal judgement is dead. Picking an
  option returns the VICTORY to the one Sensei who voiced it (append-only
  victories.jsonl beside the content, never inside), graduation (faixa
  branca → preta) derives from use, and the dojo UI ranks by victories.
  Reference Senseis keep a write-once photo under base/; senseiSanity()
  measures the evolved brain against the balanced original — the Pilot's
  own defence against teaching his brain badly. Migration was mechanical
  and versioned: mentors/ → senseis/ with history, crafts derived,
  ownership assigned (the real library migrated live: Guardião da Palavra
  Falada v3, its seed owned).
- **Effort profile (D).** project.effortProfile {questions, options,
  execution}, set at creation, editable in Agora; the per-launch slider
  overrides; automatic re-consults run at the Pilot's questions level —
  his explicit standing choice, above the automation clamp if he says so.
- **Visible spend (I).** The busy card now ticks a real elapsed timer and
  shows the iteration's tokens growing as work orders land.
- **Table size (J).** 2–4 options, the Pilot's explicit control (default
  3 — his ask), each extra option = 1 extra call, cost in the probe.
- **The base Guild (F).** preload-guild.ts ships 5 reference Senseis
  (cozinha, histórias infantis, discursos, caminhos, aprendizagem) with
  20 state-of-the-art seeds as CANDIDATES — only the Pilot admits; the
  library itself stays private and gitignored, the code is the vehicle.
- **The extractor (E).** metrics.ts, read-only: interview, tokens by
  kind/effort, decisions (sensei-voiced vs generic picks), seed
  applications from manifests, victories, pilot acts, wall clock — the
  foundation of the governed-product-vs-single-prompt comparison. NEVER
  to be forgotten (the Pilot's words).

**Honest notes:** (1) the Guardião migrated with domains [] (its seed was
"universal" pre-reform) — the Copilot assigned its craft via the governed
API (sensei_saved event, v3); the Pilot should audit those domains;
(2) the first test's 17 seed applications were the pre-reform transversal
behaviour — post-reform that number becomes craft-scoped and smaller,
which is the point; (3) the 5 reference Senseis ship with EMPTY
compositions in their base photos (seeds are candidates until admitted) —
so sanity will honestly show everything the Pilot admits as "learned";
(4) verification ran on a second shell instance (PRODUCT_PORT=4901,
fake runtime) against the real migrated library — the Pilot's live :4900
was never touched and still runs pre-reform code until he restarts it.

---

## Session 014 — 2026-07-12 (night, cont.): live execution observability — the App must never look frozen

**Trigger.** Mid-test, the Pilot's architectural hand (GPT) sent a parecer
at 18:51–18:56 (docs/reviews/PARECER-GPT-2026-07-12-OBSERVABILIDADE.md):
while Claude Code works, the App looks stopped — a product defect even when
the process is healthy; and some points were being asked a SECOND time.
Diagnosis confirmed the parecer's own thesis: the reform HAD the timer, but
the live :4900 process (and the Pilot's open browser page) predated it —
there was no way to SEE which version was running. The symptom proved the
defect.

**Executed (commits 9b54c0f parecer, 4c94707 implementation, 56dee5b fix):**
- **A — the running version is visible.** Build SHA + process start time in
  the header, home and Audit; staleness vs the repo HEAD (30s cache) with
  the parecer's exact sentence; never auto-restart.
- **B/D — the live operation card.** Honest phases (na fila → a lançar →
  a aguardar o modelo → a processar), work order N of M with its ID, model +
  effort, elapsed and last-activity ticking every second; heartbeat derives
  from REAL child-process stdout/stderr chunks — no invented percentage.
- **C — honest tokens.** "Os tokens desta chamada aparecem quando ela
  terminar"; the iteration total keeps growing per completed work order.
- **E — Pilot control.** "Parar esta operação": AbortSignal per operation,
  the cli port kills only its own child, completed work orders stay,
  operation_cancelled lands in evidence with actor pilot, and the message
  distinguishes a governed halt from a failure.
- **F — connection honesty.** Verified live that a dead server does NOT
  reject the poll fetch — it hangs and starves Chrome's connection pool, so
  a catch-driven banner would lie by omission. Fix: 5s abort per poll; the
  banner derives from the AGE of the last good poll. No user text is ever
  lost: the re-render snapshots EVERY input generically.
- **G/E-metrics — test-condition disclosure.** metrics.ts opens with the
  conditions the records prove: model/effort per work-order kind, exact vs
  estimated tokens, cancellations, timeouts. Run on the Pilot's story
  project it already testifies: everything haiku/low, 47 estimated token
  records, and 30 reconsults vs 4 consults — the runaway interview is now
  MEASURED, not just felt (its containment remains owed, per the parecer:
  haiku may ask worse questions; only the Kernel can allow infinity).
- **H — verified in the browser** on :4901 with PRODUCT_FAKE_DELAY_MS=6000
  (launch config committed): all 11 checks of the parecer pass, including
  kill-the-server banner + recovery and mid-flight cancellation.

**Method note.** First session under the Pilot's fan-out directive: a sonnet
subagent implemented under the worker's written spec and review; the worker
verified independently (smoke 96/96, tsc strict, browser proof) and
committed. The live :4900 was restarted onto 56dee5b and now answers
`build.sha=56dee5b, stale=false` on its own API — the Pilot never guesses
again.

**Honest notes.** (1) The interview-growth containment, three-questions-per-
submission, aggregated reconsult, Project Review on close and Mode Edit from
the GPT appendix are NOT in this session — they arrive as the consolidated
prompt the Pilot announced; only the two blocks delivered verbatim were
executed. (2) Cancellation marks the interrupted work order through the
kernel's normal error path (kernel.ts untouched); a dedicated
interrupted-status on the WO record is owed when the kernel next opens.
(3) The A/B haiku-vs-stronger comparison rubric is registered in the parecer
doc and waits on the baseline run (roadmap item 3).
